radius = float(input('enter the radius of ur cylinder already!'))
height = float(input('enter the height of ur cylonder already!'))
pi = 3.14
vol = radius*radius*height*pi
print('the volume of the cylinder is',vol,'units cubed')
